# --- Application Settings ---
# The traffic limit in GB. If a server's monthly egress exceeds this, it will be shut down.
TRAFFIC_THRESHOLD_GB=100
# The database connection string. Default is a local SQLite file.
DATABASE_URL=sqlite:///data/gcp_guardian.db

# --- Admin Credentials & Security ---
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password

# A strong, randomly generated secret key for signing JWTs.
# You can generate one using: openssl rand -hex 32
SECRET_KEY=your_randomly_generated_32_byte_hex_secret

# JWT Algorithm and Expiration
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=180

# --- Server Configurations ---
# You can define multiple servers by incrementing the number (1, 2, 3, ...).
# Each server must have a PROJECT_ID, VM_INSTANCE_ID, VM_ZONE, and a base64-encoded SA_KEY.

# To get the base64 encoded key, run:
# base64 -i /path/to/your/service-account-file.json

# --- Server 1 ---
GCP_SERVER_1_PROJECT_ID=your-gcp-project-id-1
GCP_SERVER_1_VM_INSTANCE_NAME=your-vm-instance-name-1
GCP_SERVER_1_VM_ZONE=us-central1-a
GCP_SERVER_1_SA_KEY=ewogICJ0eXBlIjogInNlcnZpY2VfYWNjb3VudCIsCiAgInByb2plY3RfaWQiOiAi...Cg==

# --- Server 2 (Example) ---
# GCP_SERVER_2_PROJECT_ID=your-gcp-project-id-2
# GCP_SERVER_2_VM_INSTANCE_NAME=your-vm-instance-name-2
# GCP_SERVER_2_VM_ZONE=europe-west1-b
# GCP_SERVER_2_SA_KEY=ewogICJ0eXBlIjogInNlcnZpY2VfYWNjb3VudCIsCiAgInByb2plY3RfaWQiOiAi...Cg==
